import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.ictbz.capacitorstarter',
  appName: 'Capacitor Starter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
